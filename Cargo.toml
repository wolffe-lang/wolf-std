[workspace]
resolver = "2"
members = ["xtask"]

# The library itself is wolf source under std/ — no Rust crate ships from
# this repository (D34: users invoke the single `wolf` binary, never
# anything built here). The workspace exists for the test rig only.
