//! check: run(exit=trap(assert))
//! phase: run
//! conforms: std.testing, conf.trap.map
//!
//! `todo` names what remains to build and traps `assert` when reached.

use std.testing

fn main() -> !int {
    testing.todo("the sc02 container story")
    0
}
