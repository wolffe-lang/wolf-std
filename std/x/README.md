# std.x — the nursery (D31)

**The banner, which every resident's module header also carries.** A module
under `x` is a RESIDENT, not a member: `std.x.*` is not `std.*`. Its path,
its names, its rows and its behaviour may change or disappear in a single
sprint. No other std module may depend on one; a resident MAY depend on the
facade. This file is the register — every resident is listed below with the
measured reason it is not in the facade and the named trigger that graduates
it.

**The graduation clock.** At every campaign closeout each resident is
reviewed and exactly one of three things happens, recorded in that closeout:

- it **GRADUATES** — it moves under `std.`, and because the path *is* the
  API, the move is the whole release note;
- it is **DELETED** — the reviewed contract in its header is what survives;
- it is **EXTENDED** by one campaign with a written reason.

Nothing lives in `x` unexamined, and "still useful" is not a reason. A named
trigger is. The normative statement of all of this is API-CONVENTIONS §14's
sc10 amendment; this file is where it is applied.

**A resident is not an experiment.** Every one of them is a complete, tested
body kept out of the facade because **one refused or rejected body costs
every importer of its module a lane**. Budgeting a module by its worst body
is house doctrine (sc01), and this directory is where the bill goes instead.

**Two rules writing sc10's resident added.**

1. **Check a resident's dependencies against its own LANES before writing a
   function.** A resident often has one executing lane. A delegate that is
   refused on exactly that lane leaves a function with no runnable test and
   no fenceable doc example — a claim rather than code.
   `std.x.json.float_at` was written and withdrawn inside one sprint for
   that (F-0061). A refusal that costs one lane is a ledger row; a refusal
   that costs a module's only lane is a withdrawn function.
2. **A resident and its facade successor CAN be imported by one program,
   as of the sc15 pin.** For five sprints module identity was the last
   path segment (F-0034) and the pair was `E0306` on both compiler rungs
   (F-0058); s108 probe-closed wolf-lang#29 — identity is the FULL path,
   the leaf clash is only a binding clash, and `use std.x.json as xj`
   names the second binding. Re-measured at 1b149ba: the pair resolves on
   all three lanes, and what still separates `std.x.json` from `std.json`
   is their LANES (the DOM's generic bodies refuse on the compiler rungs,
   the kernels refuse under lupin), so their tests remain single-module
   for lane reasons and both headers say which. The constraint on D31's
   graduation story is retired; the division-by-direction rule survives
   as documentation hygiene while the two surfaces coexist.

## Residents

| module | fns | why it is here | graduates when |
|---|---|---|---|
| `list_eq` | 6 | `std.list`'s element-comparing family needs `cmp.Eq`; importing `std.cmp` used to flip every std.list wolfc row to `unsupported` (F-0012), and trait dispatch executes nowhere (F-0002 / F-0004) | dispatch runs — wolf-lang#5 / #12 |
| `deque_int` | 11 | the monomorphic proof that `std.deque`'s contract is implementable; `struct X[T]` does not parse (F-0011) | wolf-lang#11 / s16 |
| `option_flatten` | 1 | `flatten`'s nested row (`T ! {none} ! {none}`) is `fail(E0201)` at parse in wolfc (F-0039) | wolf-lang#34 |
| `option_expect` | 1 | `expect`'s diverging handler needs a bottom type; wolfc says `E0401: this is (), but the else fallback must produce T` (F-0040) | wolf-lang#35 |
| `testing_text` | 1 | a builtin `str` method used to make every importer `unsupported` at resolve, which would take `std.testing`'s whole floor off the compiler lane (F-0018) | wolf-lang#17 — **re-measure at the next closeout: the `str` ceiling is gone, so this one may simply graduate** |
| `crypto/sha2` | 25 | **the D53 crypto ladder's first rung** (campaign stdc-crypto, entered at sc16): SHA-256/384/512 from FIPS 180-4, HMAC (FIPS 198-1) and HKDF (RFC 5869), gated on the vendored public corpora (NIST CAVP byte-oriented, RFC 4231, RFC 5869, Wycheproof v1 — the per-family counts are in the cavp_*/rfc*/wycheproof_* test headers and the ledger section). In the nursery because D53's ladder is OPEN (`std.crypto`'s final shape — module split, generic-over-digest HMAC, the c28 ct tier — is unruled). The lane-shape reason RETIRED at the sc18 pin: F-0091 closed (s111), the cores are three-lane, and only the biggest vector files stay off the checked tier on its step/shadow-memory budgets (the ledger's sc18 bump note). HMAC and HKDF live inside the module because a resident may not depend on a resident; the graduation move splits them. | c28's ct tier exists to verify the recorded `ct:` obligations, or the AEAD rung forces `std.crypto`'s module shape — whichever ruling arrives first |
| `crypto/chacha20` | 7 | **the D53 crypto ladder's second rung** (campaign stdc-crypto, entered at sc17): ChaCha20, Poly1305 and AEAD_CHACHA20_POLY1305 from RFC 8439 — the cipher that needs no tables, chosen because its constant-time story is true by construction in portable code — gated on the RFC's complete vector set (28) and the full Wycheproof `chacha20_poly1305_test.json` (256 valid + 60 invalid asserted, 9 InvalidNonceSize answered by the documented trap). In the nursery for sha2's reason: D53's ladder is OPEN (the lane-shape half RETIRED at the sc18 pin — F-0091/#130 closed with s111 and the whole module is three-lane now, Wycheproof parts included). Poly1305 lives inside because a resident may not depend on a resident; the graduation move splits them. NO random-nonce surface (no entropy story; the D-question lives with the rung that needs it). | c28's ct tier exists to verify the recorded `ct:` obligations (`open`'s tag compare is its named FIRST target), or the AES-GCM rung's D-question forces `std.crypto`'s module shape — whichever ruling arrives first |
| `crypto/curve25519` | 8 | **the D53 crypto ladder's third rung** (campaign stdc-crypto, entered at sc18): X25519 key exchange (RFC 7748) and Ed25519 signatures (RFC 8032) over the shared field 2^255-19 — chosen before P-256 because both curves share one field, are specified implementation-grade, and are constant-time by construction in portable code (the Montgomery ladder's cswap is arithmetic, c28's named verification target). Field in ten checked-`int` 25.5-bit limbs (the sc18 probe delta's verdict — 51x5 overflows), so THREE-LANE by construction: the field, X25519 and Ed25519 verify/sign all run on the checked tier, and only the biggest vector programs (the 1000-chain, the 40-vector Wycheproof parts) exceed its step budget. Gated on RFC 7748 §5.2/§6.1 + the full Wycheproof x25519 (518, every flag decided, reject-on-zero as the `zero` row per RFC 8446 §7.4.2), RFC 8032 §7.1 + the full Wycheproof ed25519 (151). Ed25519's SHA-512 is `std.x.crypto.sha2`'s through the module boundary — a resident-on-resident dependency the graduation move must resolve. `pub` surface: `x25519`, `x25519_base`, `public`, `sign`, `verify`, `field_self_test`, `to_hex` (+ the field/point internals). Deferred as named follow-ons: a constant-time (Montgomery-ladder or fixed-window) Ed25519 scalarmul, and a dedicated field squaring. | c28's ct tier exists to verify the recorded `ct:` obligations (`fe_cswap` is its named cswap target; SIGNING's scalarmul bit-test and `floordiv` sign correction are enumerated), or `std.crypto`'s module shape is forced — whichever ruling arrives first |
| `crypto/p256` | 11 | **the D53 crypto ladder's WEIERSTRASS rung** (campaign stdc-crypto, entered at sc23): the NIST P-256 curve (secp256r1) and ECDSA — FIPS 186-5, SEC1, SP 800-186, RFC 6979 — the curve real-world CAs issue, which makes sc22's `unsupported_alg` a real verification. THE DESIGN RULING (human-approved, constant-time): scalar multiplication is a TABLE-FREE ladder — 256 fixed iterations of complete-double / complete-add / ARITHMETIC point selection over the Renes–Costello–Batina complete formulas (a = -3), the secret bit a multiplier never a table index — so it is constant-time BY CONSTRUCTION in portable wolf (c28's verification target, `scalar_mul`), slower than a windowed table and provably ct rather than hoping. The field is checked-`int` in sixteen 16-bit Solinas limbs (the fast reduction 2^256 ≡ 2^224 - 2^192 - 2^96 + 1; 16 bits is the one width that both keeps a column < 2^36 and limb-aligns the fold — stated before the field was written, the sc18 bound-proof discipline; worst column 2^35.4, worst carry 2^48.0, ~32768x under int_max, fuzzed limb-for-limb against a big-int reference). The scalar field mod n is base-2^16 Montgomery (n is not Solinas). Nonces are RFC 6979 DETERMINISTIC (no entropy, no reuse hazard, byte-exact testable — the ruling's lean, justified; os_random is named residue). LANE SHAPE, the distinction from curve25519: a P-256 scalar mult is the COMPLETE formulas AND a double-and-add every step (~2x the interpreter cost of a Montgomery ladder), so ONE scalar mult exceeds lupin's 50M evaluation-step budget and the checked tier's — every sign/verify/vector file is NATIVE-ONLY (`unsupported`, a step-budget verdict, NOT `slow`); the FIELD / DER codec / early-reject (invalid-curve + r/s range) / trap files are THREE-LANE (their executed paths never reach a ladder). Gated on RFC 6979 A.2.5 (byte-exact k/r/s), NIST CAVP FIPS 186-4 SigVer+SigGen [P-256,SHA-256] (15+15), and the full Wycheproof ecdsa_secp256r1_sha256 (484, invalid-curve/edge-r/s/DER/malleability — all native-green). SHA-256 + RFC 6979's HMAC are `sha2`'s through the boundary — a resident-on-resident edge the graduation move must resolve (beside curve25519's). `pub` surface: `public_key`, `sign`, `verify`, `verify_der`, `signature_der`, `field_self_test`, `self_test`, `to_hex`. Named residue: masked-table / hardware-intrinsic fast P-256 (its own D-question), ECDH over P-256, a dedicated field squaring, os_random nonces. | c28's ct tier verifies the recorded `ct:` obligations (`scalar_mul` is its named table-free target; `fe_select`, the RFC 6979 chain, and the noted exceptions are enumerated), or `std.crypto`'s module shape is forced — whichever ruling arrives first |
| `jose` | 10 | **the D53 crypto ladder's FOURTH rung** (campaign stdc-crypto, entered at sc19): the JOSE envelope ACME needs, over sc18's Ed25519 — JWS signing/verification with EdDSA (RFC 7515 + RFC 8037, compact and flattened-JSON), the OKP Ed25519 public JWK and its RFC 7638 thumbprint (RFC 7517 + 7638), and the ACME request envelope (RFC 8555 §6.2, the `jwk`/`kid` protected headers and the signed body). PLUMBING, not new crypto: every signature is `curve25519.sign`/`verify`, every digest is `sha2.sum256`, every base64url is `std.base64`'s §5 pair (no pad), reused not re-landed. Vectors: RFC 8037 A.4/A.5 (JWS byte-exact), RFC 8037 A.3 + RFC 7638 §3.1 (thumbprint byte-exact), a base64url alphabet/no-pad negative set, and the frozen ACME body fixture. The framing is public-data string work (three-lane by base64/sha2's reasons); only the sign/verify paths carry a scalar multiplication and inherit the curve module's lane shape (the RFC-vector file is native + lupin-`slow`). In the nursery because D53's ladder is OPEN and this rung depends on TWO sibling residents — `crypto/curve25519` and `crypto/sha2` — resident-on-resident edges the graduation move to `std.jose`/`std.crypto` must resolve (named beside curve25519's own sha2 edge). ct: signing inherits sc18's `curve25519.sign` path verbatim (no new secret handling); the JOSE framing is public-data. `pub` surface: `signing_input`, `jws_sign_compact`, `jws_verify_compact`, `jws_flattened`, `jwk_ed25519_public`, `jwk_thumbprint_input`, `jwk_thumbprint`, `acme_protected_jwk`, `acme_protected_kid`, `acme_body`. The ws06 seam (RFC 8555 §6.2) is FROZEN in the module header: ws06 owns the HTTPS conversation and nonce/order flow, sc19 owns the signed request body. | `std.crypto`'s module shape is forced (which resolves the two resident-on-resident edges), or ACME graduates to `std.acme`/`std.jose` when wws's ws06 consumes the frozen seam — whichever ruling arrives first |
| `tls/record` | 21 | **the stdc-tls campaign's FIRST rung** (entered at sc20): the TLS 1.3 record layer and its key-schedule primitives (RFC 8446 §5 + §7.1/§7.3) — the protocol the whole D53 crypto ladder was built for (the c26/D53 road to wolf-wws W4). HKDF-Expand-Label + Derive-Secret over sc16's HKDF (the exact `"tls13 "` HkdfLabel encoding), traffic key/iv derivation (§7.3), the per-record nonce (§5.3, IV-XOR-seq right-aligned), and TLSCiphertext seal/open (§5.2, AAD = the record header; §5.4 padding strip; verify-before-expose). PLUMBING, not new crypto: the key schedule is sc16's HKDF-SHA-256, the record AEAD is sc17's AEAD_CHACHA20_POLY1305 (the MTI, §9.1). THREE-LANE by construction (only `reject_tampered_row`'s checked column is `unsupported` — the mem tier cannot model iterating the AEAD output on the tag-FAILURE path; native runs it). Vectors: RFC 8448 §3 byte-for-byte (every derived secret, traffic key, IV and the `info`/HkdfLabel dumps) + ChaCha20-Poly1305 record fixtures over RFC 8448's real traffic secrets (RFC 8448's own records are AES-128-GCM, a separate rung — F-0094). Depends on TWO sibling residents — `crypto/sha2` (HKDF) and `crypto/chacha20` (AEAD) — resident-on-resident edges the graduation move to `std.tls`/`std.crypto` must resolve (named beside jose's two edges). ct: the key schedule handles SECRETS (traffic secrets, keys, IVs) and branches only on public quantities; `record_nonce` does wrapping-not-checked arithmetic on the write IV; `open`'s padding scan reads the deprotected length (all marked for c28). NOT the handshake — sc21 produces the secrets; this rung takes them as inputs. `pub` surface: `hkdf_label`, `expand_label`, `derive_secret`, `extract`, `traffic_key`, `traffic_iv`, `finished_key`, `record_nonce`, `record_header`, `additional_data`, `inner_plaintext`, `seal`, `open`, `content_type_of`, `content_of`, `content_type_application_data`/`_handshake`/`_alert`, `to_hex`. **The sc21-consumable seam is FROZEN** in the module header: GIVEN keys -> sealed/opened records — sc21 owns transcript hashes and the state machine, sc20 owns the key derivation and the framing. | `std.crypto`/`std.tls`'s module shape is forced (resolving the two resident edges), or sc21's handshake consumes the frozen seam — whichever ruling arrives first |
| `tls/handshake` | 54 | **the stdc-tls campaign's SECOND rung** (entered at sc21): the TLS 1.3 handshake (RFC 8446 §4 + §4.4 + §7.1) over sc20's record layer — the cryptographic handshake that turns RFC 8448's message flow into the traffic keys the record layer serves under (the c26/D53 road to wolf-wws W4). The handshake message structs + exact wire serialization (ClientHello/ServerHello/EncryptedExtensions/Certificate-opaque/CertificateVerify/Finished and the trace's extensions), the running transcript hash (§4.4.1, SHA-256 over exact wire bytes incl. the 4-byte handshake header, in order), the full key schedule (§7.1, early -> handshake (EC)DHE -> master + every traffic/exporter/resumption secret), Finished (§4.4.4, HMAC over the transcript with `finished_key`, `ct_eq` compare), and the §4.1 client/server STATE MACHINE (a pure transition that rejects out-of-order/illegal flights by name — the `unexpected_message` row). PLUMBING, not new crypto: the transcript + Finished are sc16's SHA-256/HMAC, the (EC)DHE is sc18's X25519, the key schedule + `finished_key` are sc20's `record.*`. THREE-LANE by construction (all ten tests run on lupin/wolfc/native; no `slow` — the two X25519-bearing files are ~5.0s/~3.9s under lupin 0.1.13, F-0093, far under ceiling). Vectors: RFC 8448 §3 byte-for-byte — every handshake message, every intermediate transcript hash (empty, CH..SH, CH..CertVerify, CH..serverFin, CH..clientFin), the whole early->master schedule with the X25519 agreement both directions, both Finished MACs — plus hand-built negatives (out-of-order flight, tampered Finished, all-zero key_share) and two contract traps. **Certificate is OPAQUE here** (hashed + carried DER, no ASN.1 parse) and **CertificateVerify's signature is CARRIED not verified** — `certificate_verify_content` builds the §4.4.3 signed octets, but RSA-PSS/ECDSA verification is DEFERRED to sc22 (the trace signs rsa_pss_rsae_sha256 / the client-auth trace ecdsa_secp256r1, and neither exists in the D53 ladder yet — only Ed25519/X25519, so nothing about peer identity is trusted at this rung). Depends on THREE sibling residents — `tls/record` (the key schedule + the seam it feeds), `crypto/sha2` (transcript + Finished) and `crypto/curve25519` (X25519) — resident-on-resident edges the graduation move to `std.tls`/`std.crypto` must resolve (named beside record's and jose's edges). ct: message serialization, the transcript and the state machine branch only on PUBLIC quantities (bytes, lengths, message types); the schedule/Finished carry SECRETS through record/sha2/curve25519's own `ct:` lines and add no secret-dependent branch; `ct_eq` is the Finished compare (accumulate-then-single-check, a c28 target). `pub` surface (53 fns + `HandshakeResult`): `handshake`, the `hs_*` type constants, `ext`/`ext_server_name`/`ext_server_name_ack`/`ext_renegotiation_info_empty`/`ext_supported_groups`/`ext_session_ticket_empty`/`ext_key_share_client_x25519`/`ext_key_share_server_x25519`/`ext_supported_versions_client`/`ext_supported_versions_server`/`ext_signature_algorithms`/`ext_psk_key_exchange_modes`/`ext_record_size_limit`, `client_hello`/`server_hello`/`encrypted_extensions`/`certificate`/`certificate_verify`/`certificate_verify_content`/`finished_message`, `transcript_new`/`transcript_add`/`transcript_hash`, `ecdhe_shared`/`early_secret`/`handshake_secret`/`master_secret`/`client_handshake_traffic`/`server_handshake_traffic`/`client_application_traffic`/`server_application_traffic`/`exporter_master`/`resumption_master`, `finished_verify_data`/`finished_for`/`ct_eq`/`verify_finished`/`accept_finished`, `role_client`/`role_server`/`hs_expected`/`hs_step`/`hs_client_flight_complete`/`hs_server_complete`, `handshake_result` (the seam constructor — a module-qualified struct literal does not parse across the boundary, E0201, so the seam is a function as `sha2.new256` is), `concat`, `to_hex`. **The ws05-consumable seam is FROZEN** in the module header: a completed handshake yields a `HandshakeResult` (the negotiated `cipher_suite` + the four traffic secrets + exporter/resumption masters) that wolf-wws ws05 hands to sc20's record layer (`record.traffic_key`/`traffic_iv` per secret) — sc21 owns the transcript, schedule and flow, ws05 owns the socket conversation, sc20 owns the record AEAD. | `std.crypto`/`std.tls`'s module shape is forced (resolving the three resident edges), or sc22 (X.509 + CertificateVerify signature) and ws05 (the socket integration + OpenSSL interop) consume the frozen seams — whichever ruling arrives first |
| `tls/cert` | 14 | **the stdc-tls campaign's THIRD rung** (entered at sc22): X.509 v3 certificate parsing and Ed25519 chain validation (RFC 5280 over X.690 DER + RFC 8410) — the rung that makes sc21's opaque Certificate bytes MEAN something. A grammar-directed DER decoder that is the sprint's robustness flagship: every length validated against the remaining window before any content byte is read (indefinite, non-minimal, overrun, reserved, truncated each a clean NAMED `der` rejection — never an OOB read, never a trap; no unbounded recursion exists, so nesting bombs cost nothing), gated on an 18-variant one-clause-each authored conformance corpus (vendor/vectors/pki, from-scratch RFC 8032 + X.690 reference cross-checked by openssl 3.6.3) plus 7 runtime mutation classes. The RFC 5280 §4.1 structure parses fully: version (v3; v2 and encoded DEFAULTs reject), serial (§4.1.2.2 positive/minimal/<=20), the §4.1.2.3 TBS/outer algorithm equality, DNs with X.690 §11.6 SET-OF order + §4.1.2.4 CN string types, §4.1.2.5 times (2-digit window, mandatory seconds+Z, the 2049/2050 UTCTime/GeneralizedTime split BOTH ways), SPKI (RFC 8410 no-params 32-byte Ed25519), basicConstraints/keyUsage/subjectAltName decoded, unknown critical extensions RECORDED (validation rejects by name per §4.2's MUST), non-critical carried. Chain validation (§6.1): signature-by-issuer over the captured TBS bytes — Ed25519 (sc18's `curve25519.verify`) and, at sc23, ECDSA-P256 (sc23's `p256.verify_der`, ecdsa-with-SHA256 over a prime256v1 key) — validity vs a SUPPLIED `now` (compact yyyymmddhhmmss — deterministic, never wall-clock), name chaining by exact DER Name bytes, BC/keyCertSign/pathLen issuer checks, leaf SAN vs hostname (exact case-insensitive; RFC 6125 wildcards fail CLOSED, named residue). THE HONESTY SPLIT: RSA certificates PARSE structurally and validation answers the NAMED `unsupported_alg` rejection, never a silent accept (RSA is the last unsupported-alg rung); an ECDSA-P256 cert now VALIDATES (a bad P-256 signature rejects `bad_signature`, witnessed). The Ed25519 path stays THREE-LANE (checked-int); the ECDSA path is NATIVE-ONLY (a P-256 verify is two table-free ladders past lupin/checked step budgets — the p256 lane note), and RSA's `unsupported_alg` is three-lane (no scalar mult on that path). ct: PUBLIC DATA throughout — a certificate carries no secret, stated per fn (the sc18 verify-is-public rule). Depends on TWO sibling residents — `crypto/curve25519` (Ed25519 verify) and `crypto/p256` (ECDSA-P256 verify) — resident-on-resident edges the graduation move must resolve, beside record's/handshake's/jose's edges. NAMED RESIDUE (deliberately not half-built): revocation (CRL/OCSP), RSA signature validation (its crypto rung), RFC 6125 wildcards, the CN hostname fallback, name constraints + policy machinery, non-dNSName SAN matching, non-P-256 ECDSA curves. `pub` surface: `Cert` (the parsed value), `parse`, `cert_end`, `validate_chain` (THE ws05 SEAM), `verify_signed_by`, `host_matches`, `alg_name`, `oid_ed25519`, `oid_ecdsa_sha256`, `oid_ec_public_key`, `ku_digital_signature`/`ku_key_cert_sign`, `timestamp`, `to_hex`. **The ws05-consumable seam is FROZEN** in the module header: peer chain (concatenated DER, leaf first — exactly sc21's cert_data entries joined) + hostname + trust anchor + supplied now -> the validated parsed LEAF (subject, SAN, validity, SPKI) or one of nine named rejections (`der`, `unsupported_alg`, `bad_signature`, `expired`, `not_yet_valid`, `name_mismatch`, `not_ca`, `issuer_mismatch`, `unknown_critical`) — sc22 owns certificate meaning, sc21 owns the flow that carries the bytes, ws05 owns the socket conversation. | `std.crypto`/`std.tls`'s module shape is forced (resolving the resident edge), or the RSA/P-256 rungs land (validation widens past Ed25519), or ws05 consumes the frozen seam — whichever ruling arrives first |
| `json` | 22 | **the nursery's first tenant, and the only one D31 named in advance.** Two measured reasons, both in its header, ONE of which moved at sc15: (1) unequal lanes — the `json_*` kernels were the checked tier's alone from s40 until the sc15 pin, where s107's native crossing lit the second compiler rung; lupin still declines by design while is18 lands its own reading; (2) a dotted path is not the API json ends up with — no key enumeration (the sc15 NAMED STOP, F-0087), keys containing `.` unreachable, every read a fresh parse. sc15 added the DOM half over the same kernels: `Node` plus `root`/`node`/`member`/`element` and seven reads — the compiled lanes' navigable json, beside `std.json`'s interpreter-lane `Value` | the native mirror of `wolf_mem::json` (**FIRED at the sc15 pin — s107**) plus the interpreter's own (in flight, is18) — AND a ruling on whether a query face belongs beside `std.json`'s DOM or is deleted in favour of it |

## Clocks

- **The five stdc01 residents** (`list_eq`, `deque_int`, `option_flatten`,
  `option_expect`, `testing_text`) were each extended by one campaign at the
  sc06 closeout, on the ruling that each is blocked on a filed upstream issue
  rather than an open design question and each has a named trigger. **Their
  clock expires at the stdc02 closeout**, which is this campaign's: a
  resident still here after it is deleted, and the reviewed contract in its
  header is what survives.
- **`json` entered at sc10**, mid-campaign. Its first closeout is stdc02's,
  and the honest reading is that it will be EXTENDED there rather than
  judged: its trigger is an upstream mirror nobody has scheduled, and one
  campaign is the review interval rather than the deadline. Whatever happens,
  it is written in that closeout with a reason — which is the whole mechanism.
- **`json`'s OTHER trigger clause fired at sc15**: s107 crossed the
  `json_*` kernels natively (`wolf_rt::json`, the hand mirror of
  `wolf_mem::json` the trigger named, parity-pinned upstream), so both
  compiler rungs execute the module and only the interpreter's own reading
  (is18, concurrent) is outstanding. sc15 also grew the DOM half — `Node`
  and its navigations, over the same kernels — which RESHAPES the deletion
  question the sc14 bullet below sharpened: `std.json`'s DOM executes on
  the interpreter alone (F-0029) and this resident's on the compilers
  alone, so today each read surface is the only one its lanes have.
  Deleting the resident in favour of the DOM now costs the compiled lanes
  every json read; the honest closeout question is no longer "query face
  or DOM" but "which surface graduates when the lanes converge". Written
  here so the closeout finds it measured rather than remembered.
- **HALF of `json`'s trigger fired at sc14**, and the closeout should read it
  as evidence rather than as a verdict. The trigger has two clauses: an
  upstream mirror of `wolf_mem::json` (unmoved — the kernels are still the
  checked tier's alone), and *a ruling on whether a query face belongs beside
  `std.json`'s DOM or is deleted in favour of it*. `std.json` is now a
  complete DOM in both directions — `parse` and `stringify`, `get`/`at`,
  `escape`/`unescape` — so the second clause is answerable for the first
  time, and the question it turns into is sharp: this resident's 11 functions
  re-parse the document on every read (its own header calls that the
  second-loudest argument for the DOM), which is exactly what the DOM now
  makes unnecessary. What it still has that the DOM has not is a dotted path
  and a one-call read. That is a convenience question, not a capability one,
  and a convenience that costs a quadratic read is the kind D31's nursery
  exists to delete rather than to graduate.
