/target
# Local binary cache for the two implementations (lupin, wolf). Binaries
# are acquired, never committed; `cargo xtask doctor` reports what is
# here and whether it matches the recorded pins (vendor/tools.toml).
/.wolf-bin/
